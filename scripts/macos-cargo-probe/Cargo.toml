[package]
name = "age-phone-m0-cryptokit-probe"
version = "0.0.0"
edition = "2024"
rust-version = "1.88"
publish = false
description = "Isolated macOS M0 CryptoKit source-install experiment, not a product backend"
license = "MIT"
repository = "https://github.com/biulight/age-plugin-phone"
include = ["src/**", "native/**", "build.rs", "Cargo.toml", "Cargo.lock", "README.md"]

# Independent source-install fixture; not a fifth product crate or runtime backend.
[workspace]

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
all = "warn"
pedantic = "warn"
