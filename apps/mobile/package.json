{
  "name": "age-plugin-phone-mobile",
  "private": true,
  "version": "0.1.0-alpha.3",
  "packageManager": "bun@1.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "android:build:wifi-poc": "AGE_PLUGIN_PHONE_WIFI_POC_BUILD=1 tauri android build --debug --apk --target aarch64 --ci --config src-tauri/tauri.wifi-poc.conf.json",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
